{
  "buildCommand": "pnpm install --no-frozen-lockfile && prisma generate && next build",
  "devCommand": "next dev",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
